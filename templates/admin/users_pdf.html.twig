<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Users List</title>
    <style>
        body { font-family: Arial, sans-serif; }
        h1 { color: #3498db; text-align: center; }
        table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        th { background-color: #3498db; color: white; padding: 8px; text-align: left; }
        td { padding: 8px; border-bottom: 1px solid #ddd; }
        .footer { margin-top: 20px; text-align: right; font-size: 0.8em; color: #666; }
        .badge { padding: 2px 5px; border-radius: 3px; font-size: 0.8em; }
        .admin { background-color: #2ecc71; color: white; }
        .driver { background-color: #e67e22; color: white; }
        .rider { background-color: #9b59b6; color: white; }
    </style>
</head>
<body>
    <h1>Users Management Report</h1>
    <p>Generated on: {{ date|date('Y-m-d H:i:s') }}</p>
    
    <table>
        <thead>
            <tr>
                <th>Name</th>
                <th>Email</th>
                <th>CIN</th>
                <th>Roles</th>
            </tr>
        </thead>
        <tbody>
            {% for user in users %}
            <tr>
                <td>{{ user.nom }} {{ user.prenom }}</td>
                <td>{{ user.email }}</td>
                <td>{{ user.cin }}</td>
                <td>
                    {% if user.isAdmin %}<span class="badge admin">Admin</span>{% endif %}
                    {% if user.isDriver %}<span class="badge driver">Driver</span>{% endif %}
                    {% if user.isRider %}<span class="badge rider">Rider</span>{% endif %}
                </td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
    
    <div class="footer">
        Total Users: {{ users|length }}
    </div>
</body>
</html>