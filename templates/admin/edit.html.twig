{% extends 'admin/dashboard.html.twig' %}

{% block content %}
    <div class="form-container">
        <h1 style="margin-bottom: 25px; color: #2c3e50;">
            <i class="fas fa-user-edit"></i> Edit User
        </h1>
        
        <form method="POST">
            <div class="form-group">
                <label>First Name:</label>
                <input type="text" name="nom" value="{{ user.nom }}" required>
            </div>

            <div class="form-group">
                <label>Last Name:</label>
                <input type="text" name="prenom" value="{{ user.prenom }}" required>
            </div>

            <div class="form-group">
                <label>Email:</label>
                <input type="email" name="email" value="{{ user.email }}" required>
            </div>

            <div class="form-group">
                <label>CIN:</label>
                <input type="text" name="cin" value="{{ user.cin }}" required>
            </div>

            <div class="form-group">
                <label>
                    <input type="checkbox" name="isAdmin" {% if user.isAdmin %}checked{% endif %}>
                    Admin
                </label>
                
                <label style="margin-left: 20px;">
                    <input type="checkbox" name="isDriver" {% if user.isDriver %}checked{% endif %}>
                    Driver
                </label>
                
                <label style="margin-left: 20px;">
                    <input type="checkbox" name="isRider" {% if user.isRider %}checked{% endif %}>
                    Rider
                </label>
            </div>

            <div class="form-group">
                <button type="submit" class="btn btn-edit" style="padding: 12px 25px;">
                    <i class="fas fa-save"></i>
                    Save Changes
                </button>
                <a href="{{ path('admin_users') }}" class="btn" style="background: #95a5a6; color: white;">
                    Cancel
                </a>
            </div>
        </form>
    </div>
{% endblock %}