{% extends 'base.html.twig' %}

{% block title %}Modifier le Forum{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="fw-bold text-primary">Modifier le Forum</h1>
    </div>

    {# Afficher un message de succès ou d'erreur après la soumission du formulaire #}
    {% if app.session.flashbag.has('success') %}
        <div class="alert alert-success">
            {{ app.session.flashbag.get('success')[0] }}
        </div>
    {% elseif app.session.flashbag.has('error') %}
        <div class="alert alert-danger">
            {{ app.session.flashbag.get('error')[0] }}
        </div>
    {% endif %}

    <!-- Formulaire de modification du forum -->
    <div class="card shadow-sm rounded p-4">
        <h4 class="mb-4">Veuillez modifier les informations du forum :</h4>
        
        {# Formulaire avec des champs améliorés #}
        {{ include('forum/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    </div>

    <div class="mt-4">
        <a href="{{ path('app_forum_index') }}" class="btn btn-outline-primary btn-lg d-flex align-items-center">
            <i class="bi bi-arrow-left me-2"></i> Retour à la liste des forums
        </a>
    </div>

    {# Formulaire de suppression du forum dans un bloc séparé pour éviter toute confusion #}
    <div class="mt-4">
        <h5 class="text-danger">Supprimer le Forum</h5>
        <p>Si vous souhaitez supprimer ce forum, cliquez sur le bouton ci-dessous. Cette action est irréversible.</p>
        
        <!-- Bouton de suppression stylisé -->
        {{ include('forum/_delete_form.html.twig') }}
    </div>
</div>

{# Ajout des styles supplémentaires pour les formulaires et les boutons #}
<style>
    .form-control {
        border-radius: 10px;
        padding: 15px;
        font-size: 1.1rem;
    }
    .btn-lg {
        font-size: 1.1rem;
        padding: 10px 20px;
        border-radius: 8px;
    }
    .btn-outline-primary {
        color: #007bff;
        border-color: #007bff;
        transition: background-color 0.3s, color 0.3s;
    }
    .btn-outline-primary:hover {
        background-color: #007bff;
        color: white;
    }
</style>

{% endblock %}
