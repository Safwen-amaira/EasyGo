{{ form_start(form, {'attr': {'class': 'reclamation-form'}}) }}
  <div class="card reclamation-card">
    <div class="card-body">
      <h5 class="card-title">{{ button_label|default('Save') }} Réclamation</h5>
      <div class="form-content">
        <div class="mb-3">
          {{ form_label(form.categorie, null, {'label_attr': {'class': 'form-label'}}) }}
          {{ form_widget(form.categorie, {'attr': {'class': 'form-control'}}) }}
          {{ form_errors(form.categorie, {'attr': {'class': 'text-danger'}}) }}
        </div>
       
        <div class="mb-3">
          {{ form_label(form.description, null, {'label_attr': {'class': 'form-label'}}) }}
          {{ form_widget(form.description, {'attr': {'class': 'form-control', 'rows': '4'}}) }}
          {{ form_errors(form.description, {'attr': {'class': 'text-danger'}}) }}
        </div>
       
      </div>
      <div class="d-flex justify-content-end">
        <button type="submit" class="btn save-btn">{{ button_label|default('Save') }}</button>
      </div>
    </div>
  </div>
{{ form_end(form) }}