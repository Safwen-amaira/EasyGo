{% extends 'base.html.twig' %}

{% block title %}Nouvelle Réclamation{% endblock %}

{% block body %}
  <div class="container my-5">
    <h1 class="text-center mb-4" data-aos="fade-up" style="color: #007bff;">Créer une nouvelle réclamation</h1>

    <div data-aos="fade-up" data-aos-delay="200">
      {{ include('reclamation/_form.html.twig', {'button_label': 'Créer'}) }}
    </div>

    <div class="mt-3 text-center" data-aos="fade-up" data-aos-delay="300">
      <a href="{{ path('app_reclamation_index') }}" class="btn btn-outline-primary">Retour à la liste</a>
    </div>
  </div>
{% endblock %}
