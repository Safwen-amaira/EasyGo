{% extends 'base.html.twig' %}

{% block title %}Créer une récompense{% endblock %}

{% block body %}
    <h1>Créer une nouvelle récompense</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">Créer</button>
    {{ form_end(form) }}

    <a href="{{ path('recompense_index') }}">⬅️ Retour à la liste</a>
{% endblock %}
