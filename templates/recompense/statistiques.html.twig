{% extends 'base.html.twig' %}

{% block title %}📈 Statistiques des Récompenses{% endblock %}

{% block body %}
<div class="container py-5">
    <h2 class="mb-4">📊 Tableau de Bord des Récompenses</h2>

    <div class="row mb-5">
        <div class="col-md-4">
            <div class="bg-light p-3 rounded shadow-sm text-center">
                <h6>Total des récompenses</h6>
                <h4 class="fw-bold text-primary">{{ total }}</h4>
            </div>
        </div>
        <div class="col-md-4">
            <div class="bg-light p-3 rounded shadow-sm text-center">
                <h6>Points requis moyens</h6>
                <h4 class="fw-bold text-success">{{ avgPoints }}</h4>
            </div>
        </div>
        <div class="col-md-4">
            <div class="bg-light p-3 rounded shadow-sm">
                <h6>Top 3 types les plus utilisés</h6>
                <ul class="mb-0 ps-3">
                    {% for t in topTypes %}
                        <li>{{ t.nom }} — {{ t.total }} fois</li>
                    {% else %}
                        <li class="text-muted">Aucune attribution</li>
                    {% endfor %}
                </ul>
            </div>
        </div>
    </div>

    <h4>👑 Top 5 utilisateurs les plus actifs</h4>
    <table class="table table-hover mt-3">
        <thead>
            <tr>
                <th>Utilisateur</th>
                <th>Points</th>
                <th>Trajets</th>
            </tr>
        </thead>
        <tbody>
            {% for user in topUsers %}
                <tr>
                    <td>{{ user.nomUtilisateur }}</td>
                    <td><span class="badge bg-success">{{ user.points_accumules }}</span></td>
                    <td>{{ user.totalTrajets ?? 0 }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}

{% block javascripts %}
    {# No chart-related scripts anymore #}
{% endblock %}
