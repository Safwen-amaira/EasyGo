<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Admin Dashboard{% endblock %}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <!-- Bootstrap 5 CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            min-height: 100vh;
            display: flex;
            background-color: #f8f9fa;
        }

        .sidebar {
            width: 250px;
            background: #343a40;
            color: white;
            min-height: 100vh;
        }

        .sidebar a {
            color: white;
            text-decoration: none;
            display: block;
            padding: 15px;
        }

        .sidebar a:hover {
            background: #495057;
        }

        .header {
            background: white;
            padding: 15px;
            border-bottom: 1px solid #dee2e6;
        }

        .main-content {
            flex: 1;
            display: flex;
            flex-direction: column;
        }

        .content {
            padding: 20px;
        }

        @media (max-width: 768px) {
            .sidebar {
                display: none;
            }
        }
    </style>
</head>
<body>

    <!-- Sidebar -->
    <div class="sidebar d-flex flex-column p-3">
        <h4 class="text-center my-3">Admin</h4>
        <a href="{{ path('app_forum_index') }}">📄 Liste des Forums</a>
        <a href="{{ path('app_forum_new') }}">➕ Ajouter un Forum</a>
        <!-- Ajoute d'autres liens ici si nécessaire -->
    </div>

    <!-- Main content -->
    <div class="main-content">
        <div class="header">
            <h2>{% block header %}Panneau d’administration{% endblock %}</h2>
        </div>
        <div class="content">
            {% block body %}{% endblock %}
        </div>
    </div>

</body>
</html>
